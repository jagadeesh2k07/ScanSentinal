# ScanSentinal
ScanSentinel — An AI-powered document anomaly detection system that uses OCR/ICR to extract text from scanned or handwritten documents and automatically flags suspicious patterns like invalid formats, missing fields, duplicate entries, and impossible values, generating a risk score report.
It is an AI automated document scanning module  